interface:
  display_name: "Run Tests"
  short_description: "Run a task's suite and gate on it \u2014 suite green, every\u2026"
  default_prompt: "Use $run-tests to run a task's suite and gate on it \u2014 suite green, every acceptance criterion exercised at the right level, coverage met. Use when a task's code is written and\u2026"
policy:
  allow_implicit_invocation: false
