interface:
  display_name: "Grill ML Reqs"
  short_description: "Pin the requirements for an AI/ML feature \u2014 what the model\u2026"
  default_prompt: "Use $grill-ml-reqs to pin the requirements for an AI/ML feature \u2014 what the model must do and how well, its evaluation criteria (the acceptance tests for model behaviour), the\u2026"
policy:
  allow_implicit_invocation: false
