interface:
  display_name: "Generate UI Prototype"
  short_description: "Generate a clickable HTML prototype of a UI slice's screens\u2026"
  default_prompt: "Use $generate-ui-prototype to generate a clickable HTML prototype of a UI slice's screens from the real design-system tokens and components, every interaction state included \u2014\u2026"
policy:
  allow_implicit_invocation: false
