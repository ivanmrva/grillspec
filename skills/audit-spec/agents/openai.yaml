interface:
  display_name: "Audit Spec"
  short_description: "Audit an EXISTING spec for completeness, consistency\u2026"
  default_prompt: "Use $audit-spec to audit an EXISTING spec for completeness, consistency, contradictions, and whether a coding agent can build it without guessing \u2014 the judgment layer above the\u2026"
policy:
  allow_implicit_invocation: true
