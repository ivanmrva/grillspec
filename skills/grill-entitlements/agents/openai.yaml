interface:
  display_name: "Grill Entitlements"
  short_description: "Pin the entitlement model \u2014 the access tiers, what each\u2026"
  default_prompt: "Use $grill-entitlements to pin the entitlement model \u2014 the access tiers, what each unlocks (feature gating), usage limits/quotas, and how access degrades on a lapsed billing\u2026"
policy:
  allow_implicit_invocation: false
